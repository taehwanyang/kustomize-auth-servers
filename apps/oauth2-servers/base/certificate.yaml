apiVersion: cert-manager.io/v1
kind: Certificate 
metadata:
  name: auth-local-cert
spec:
  secretName: auth-local-tls
  dnsNames:
    - ythwork.com
    - "*.ythwork.com"
  issuerRef:
    name: selfsigned-issuer
    kind: Issuer 